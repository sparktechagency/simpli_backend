/* eslint-disable @typescript-eslint/no-explicit-any */
const handlePaypalPaymentSucess = async (payment: any) => {
  console.log('console paymetn from handle paypal payment success', payment);
};

export default handlePaypalPaymentSucess;
